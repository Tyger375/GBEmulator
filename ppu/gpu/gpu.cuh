void prepare_pointers(
        Byte**,
        size_t*,
        Uint32**,
        size_t*,
        u32,
        u32,
        int
    );
void free_pointers(Byte*, Uint32*);
void build_video(
        Byte*,
        Uint32*,
        Byte*,
        Uint32*,
        u32,
        u32,
        int
    );